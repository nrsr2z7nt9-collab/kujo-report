const id=Number(new URLSearchParams(location.search).get('id'));const $=s=>document.querySelector(s);let db;
function openDB(){return new Promise((ok,no)=>{const q=indexedDB.open('kujo-report',1);q.onsuccess=()=>ok(q.result);q.onerror=()=>no(q.error);});}
function get(store,key){return new Promise((ok,no)=>{const q=db.transaction(store).objectStore(store).get(key);q.onsuccess=()=>ok(q.result);q.onerror=()=>no(q.error);});}
const species=['イノシシ','シカ','キョン','アライグマ','ハクビシン','タヌキ'],districts=['須賀','浜','高山田','久保','新町','六軒町','岩和田','実谷','七本','上布施','御宿台'];
function choices(a,v){return a.map(x=>x===v?`○${x}○`:x).join('・');}function text(s,v){$(s).textContent=v||'';}
async function load(){try{db=await openDB();const r=await get('records',id);if(!r)throw 0;text('#date',(r.capture_date||'').replace(/^\d{4}-/, '').replace('-','月')+'日');text('#hunter',r.hunter_name);text('#species',choices(species,r.species));text('#district',choices(districts,r.district));text('#location',r.location);text('#trap',r.trap_number);text('#size',r.size_cm?`${r.size_cm}cm`:'');text('#weight',r.weight_kg?`${r.weight_kg}kg`:'');text('#sex',r.sex);text('#female',`胎児 ${r.has_fetus||''}　泌乳 ${r.lactation||''}`);text('#antlers',r.antlers?`${r.antlers}本`:'');text('#method',r.capture_method==='その他'?(r.capture_method_other||'その他'):r.capture_method);text('#disposal',r.disposal==='その他'?(r.disposal_other||'その他'):r.disposal);for(let n=1;n<=3;n++){const p=await get('photos',`${id}:${n}`),f=$(`#p${n}`);if(p){const img=new Image;img.src=URL.createObjectURL(p.blob);f.append(img);}else f.textContent=`写真${n}なし`;}$('#pages').hidden=false;}catch{$('#error').hidden=false;}}
$('#print').onclick=()=>print();load();
